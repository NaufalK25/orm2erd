erDiagram

  %% Entities
  User {
    int id PK "default: autoincrement()"
    string email UK
    enum role "enum: MEMBER, ADMIN | default: MEMBER"
  }

  Post {
    int id PK "default: autoincrement()"
    string title UK "unique with: authorId"
    string? content
    int authorId FK, UK "unique with: title"
  }

  Tag {
    int id PK "default: autoincrement()"
    string name UK
  }

  %% Relationships
  User ||--o{ Post : "posts"
  Post }o--o{ Tag : "tags"