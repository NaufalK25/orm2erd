datasource db {
  provider = "postgresql"
}

model Membership {
  userId Int
  orgId  Int
  role   String

  @@id([userId, orgId])
  @@unique([orgId, role])
  @@index([role])
  @@index([userId, role], name: "user_role_idx")
}
