datasource db {
  provider = "postgresql"
}

model PostTag {
  postId  Int
  tagId   Int
  addedBy String

  @@id([postId, tagId])
  @@unique([tagId, addedBy])
  @@index([addedBy])
  @@index([postId, addedBy], name: "post_addedby_idx")
}
