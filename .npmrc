# typescript-eslint's peer range (<6.1.0) doesn't support this project's
# TypeScript 7 yet — see the lint discussion. Without this, every install
# hits the same ERESOLVE conflict regardless of what package is being added.
legacy-peer-deps=true
